# Remielle 特效设计（VFX Design）

状态：初始化骨架；架构事实见 `../Architecture/VFX_Architecture.md`。

## 当前基线

31 项粒子效果中 8 项接入；命中粒子 2 项；武器拖尾/发光/渐隐已接。闪光颜色已知不对（源 Alpha 指数已核对，问题在 HDR 显示路径）。

## 接入原则（继承自已验收阶段）

- 每一项效果先取证（层级、材质指针、贴图、时钟规则、开始偏移），再 GPU 组装，再真机生命周期检查（30/60/120 Hz）。
- 粒子实例预热，禁止运行时临时创建；打断时正常回收。
- 与原版对照以真实 Player 同帧 GPU 画面为准，不以离线截图替代。

## 待办

- [ ] 31 项效果逐项清单与优先级（`../Inventory/VFX_Index.md`）。
- [ ] 高亮饱和、alpha/dissolve 对齐方案。
