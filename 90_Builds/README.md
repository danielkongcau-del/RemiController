# 90_Builds — 构建层

## 分区

- `Dev/`：日常开发构建。
- `Test/`：验证构建（批处理审计、帧捕获用）。
- `Release/`：阶段性完整交付。
- `Captures/`：构建产出的画面捕获（截图/视频/帧序列）。

## 构建纪律（继承自 E:\ZZZ\AGENTS.md）

- 批处理渲染必须 **D3D11**，禁止 `-nographics`。
- 同一工程不得并发打开；构建期间不开编辑器。
- 构建命令与参数记录在产物旁（`build-command.txt`），保证可复现。
