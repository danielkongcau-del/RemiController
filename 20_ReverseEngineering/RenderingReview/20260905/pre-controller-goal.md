# 当前持续目标：推进至接控制器之前

目标保持为：完成原皮展示／商店风格的渲染、光照及附件显示准备，停在接控制器之前。已有动画用于检验渲染随姿态更新，不新增移动、攻击或技能控制器。

完成要求：

1. 接齐每页 24 次角色 G-buffer 绘制以及 Deferred 后额外的 Body_1 第二材质槽绘制，包括基础表面、描边、发丝生成、面部模板修正、眼睛与后续头发；验证几何、纹理、常量和渲染状态。
2. 接齐深度／法线辅助层、原生 Deferred、展示／商店光照、LUT、Bloom 和最终后处理，验证完整颜色链与可见输出。
3. 将固定捕获输入改为当前骨架、相机、分辨率、光照和上一帧的动态输入；验证正反面、面部光向、多个原生动作和裁剪。
4. 核实附件归属与显示依据；保留未确认的小环，不能用删网格掩盖问题。
5. 生成可使用的渲染场景／Player，完成实际 GPU 和运行检查，交接文档明确入口、验证范围及无法恢复的原始数据边界。

当前第 1 项的固定输入检查已覆盖两页完整 24/24 次绘制：独立 D3D11 与 Unity 的颜色、深度、模板均逐位吻合。四个 sampler 原始描述符仍缺，A/B 所需行为已由捕获输出验证；红色小纹理的两个低级 mip、数组未用第 7 层和部分生成 shader 的原始字节仍有来源边界。第 2–5 项的实时输入、可见链与最终 Player 验收继续推进，不能把固定抓帧对齐当作整个目标完成。

旧模型字节基线保持 81/82；不调用模型／动画再生成入口，不刷新旧基线掩盖历史 prefab 差异。最终报告必须保留此项，且区分渲染改动本身的验证结果。

第 3 项已推进：原始 rootBone 指针、当前 7 网格蒙皮及上一帧状态有独立来源核验；两个基础／面部 VS 的 96 组动态输入与实际 Unity 顶点阶段逐位一致。其他 VS、完整三角形栅格化、像素灯光与最终场景仍未验收，不能因这个顶点子门禁通过而结束目标。

第 1／3 项现已进入动态三角形阶段：144 次实际绘制与原始 DXBC／捕获汇编参考执行的同输入逐位一致。第 2 项已接当前原生 G-buffer → UI 角色 Deferred → 角色 LUT，六张 1920×1080 HDR 图通过有限值和完整取景检查。Deferred 动态输入仍需独立 GPU 对照，变化中的光照／阴影、辅助层、Bloom、最终后处理、附件和新 Player 尚未完成，持续目标保持进行中。

第 2 项本轮进展：动态 Deferred 六组输入与原始 DXBC 精确一致；解决深度 DSV/SRV 冲突。展示／商店完整 Bloom 及 UberPost 与原始采集逐位匹配；六张 1920×1080 当前模型最终颜色图与原始 VS/PS 同输入逐位一致。剩余 TAA 历史帧、动态光照及半分辨率辅助层、附件状态、15 动作连续帧、正式场景和 Player。目标仍进行中，旧模型基线仍为 81/82。

本轮 TAA：两页原始颜色／R8 标记逐位一致；当前输入的 48 帧原始字节码对照精确通过，42 次历史延续、6 次初始化／重置无差异，私有目标释放后无泄漏。六张 1920×1080 图已包含 TAA。输入追踪发现展示 draw 92／商店 draw 93 的 Body_1 第二材质槽为额外 338 个三角形，原始 VS／PS 和几何齐备，运行时尚未补绘。此前“24 道完整”只适用于 G-buffer 阶段，不能据此宣称整页角色链完整。详见 ui-live-binding/temporal-rendering.md。其他动态光照／辅助层、附件、完整动作 Player 验收继续推进；目标仍 active，旧模型基线 81/82 未改写。

本轮后续 Body_1 进展：源 renderer 材质指针、对应 UI 材质与 NapAvatarUITransparent 的 block+CAB+pathID 已分别核实。修复反编译 VS 的拆分寄存器输出后，596,760 个独立阶段有效值逐位一致；完整前向混合的两页 26,790,912 个半精度值／后端与采集逐位一致，深度和 alpha 不变。下一步接 Unity 和当前骨架，不能将独立 D3D11 成功等同于实时集成已完成。

2026-09-06 后续 Body_1：Unity 两页固定捕获通过，133 个命名常量字段已接入。动态原 VS／翻译 PS 对照发现整数标志被反编译成零；按原指令修复后，15 主动作 × 两页共 30 组、每后端 62,208,000 个半精度值逐位一致，深度、alpha 和运动辅助目标不变，无私有目标泄漏。完整颜色链重跑 48 帧 TAA 和六张 1080p 图通过，均已包含这道绘制。正式可见 Player、动态光照／半分辨率辅助层及附件尚未完成，目标继续 active，旧模型基线 81/82 不变。

2026-09-06 深度辅助层：两页原始输出的线性深度、法线／样本编号、范围、交错深度和模板共 10,046,592 个值逐位一致。六组当前模型／镜头／尺寸／裁剪输入通过：4,872,384 个选择／编码／归并值精确，885,888 个线性深度值与 CPU 倒数最多相邻一步，无私有目标泄漏。正确处理 D3D 半精度向零转换；下一层 mip 只按原汇编验证，原始四个运行时生成 DXBC 仍缺失。生产单元已可调用，辅助消费者与正式可见场景尚待连接。动态光照、附件、全动作连续 Player 继续推进，目标 active，旧基线 81/82 保留。

2026-09-06 动态展示灯组：复用两页捕获实体缓冲、命名常量和旧 NapRenderEntity 结构记录，主光、代理点、环境渐变及 Deferred 已同步更新。240 次当前 G-buffer、10 组 Deferred 与 10 组后续 Body_1 和原始 shader 的同输入输出逐位一致，十张 1080p 最终画面包含 16 帧 TAA。刚性跟随与转灯为明确实现的展示策略，不能称为原游戏 CPU 天气／灯光混合程序已经恢复。新数据缓冲的无关记录保持逐字节不变。正式可见原生场景／Player、辅助消费者、附件与全动作连续帧继续推进，目标保持 active；旧模型基线 81/82 未刷新。

2026-09-06：原生展示／商店新场景和 NativePlayer 已通过 100 帧编辑器相机检查、8,636 帧实际 Player 动画推进（30 个完整动作片段、30 次渐变切换），零颜色传递差异，私有目标释放完整。后台测试在正常 Animation / LateUpdate 后调度 Camera.Render，普通交互使用启用相机；不冒称已代操作 GUI 或测量交互性能。已从旧捕获核实反射消费者排除 UI 角色，并恢复 486 个节点、27 个 renderer 的源默认状态和根骨父链。附件事件与离体小环归属仍待推进；原始采样器／CPU 灯光边界保留，旧模型基线 81/82，未接控制器。

本轮第 2 项的纯角色辅助消费者范围已核实，第 5 项新增可见链与连续 Player 验收通过。第 4 项仅源默认状态和当前页面来源选择已补全，离体附件及事件时序继续推进，目标保持 active。

2026-09-06 附件补全：离体小环已由 27 次隔离 GPU 编号绘制唯一确认为 Remielle_Floater_02（2,216 顶点、8 根骨、259 个像素）。八根原始骨骼 PPtr 顺序吻合；15 动作的 20,160 条相关缩放曲线、1,703,760 个键与封存 RANI 输入逐位一致。52 份旧逆向技能文件重新校验，恢复 113 条显示／材质／抖动淡出／缩放动作，所有具名 renderer 目标均已匹配。当前原生展示链本就不绘制 Floater_02，源资产继续保留。标签竞争、modifier 触发与生命周期语义尚需核实，未接控制器；旧模型基线 81/82 不变。

第 4 项的部件身份、缩放和事件目标来源现已补全；原始标签仲裁与 modifier 生命周期尚不冒称已运行对齐。持续目标保持 active，停在接控制器之前。


2026-09-06 ??????????????????????????????? per-renderer ???????? 32,768 ???????????????? ModifierNameList ?? AirCombat_ReleaseFloater ???????????? 297 ??????????? 52 ???469 ?????198 ???253 controller ?????90 ???????????????? Player ? 1,062 ????????????? ../../RemielleControllerPreparation/20260906/README.md?readyForDisplayLocomotionController ??????????preControllerGoalComplete ??????????????? false???????????????????????????????????????sampler?CPU ???????? 81/82 ????
